New post "<%= @post.title %>" has been favorited
<%= "=" * (@post.title.length + 12) %>

By: <%= @post.user.name %>

<%= @post.body %>

You will receive updates whenever this post is commented on.

Visit online at: <%= topic_post_url(@post.topic, @post) %>
